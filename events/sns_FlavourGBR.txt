namespace = SNS_GNR

	country_event = {
		id = SNS_GBR.1
		title = SNS_GBR.1.t
		desc = SNS_GBR.1.d
		
		fire_only_once = yes
		
		trigger = {
			NOT = { has_heir_flag = SNSvictoria }
		}
		
		mean_time_to_happen = {
			days = 1
		}
		
		immediate = {
			set_ruler_flag = SNSvictoria
		}
		
		option = {
			name = SNS_GBR.1.a
			add_prestige = 10
		}
	}